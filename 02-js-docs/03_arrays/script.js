let name = "Lorena";

let names = ["Maria", "João", "Pedro"]
console.log(names); // Accessing the entire array
console.log(names[2]); // Accessing the third element of the array


//Adding elements to the array
names.push("Ana"); // Adds "Ana" to the end of the array
console.log(names);


//Removing the last element from the array
delete names[0] // Removes the first element (Maria) from the array
console.log(names); 
